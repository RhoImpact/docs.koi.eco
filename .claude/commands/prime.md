Read and internalize the project's CLAUDE.md, then review these key files for current project state:

1. `next.config.mjs` -- build config, env vars
2. `tailwind.config.ts` -- theme/color customization
3. `src/shared/navigation-data.ts` -- all routes and sidebar nav structure
4. `src/components/mdx.tsx` -- MDX component mappings (what's available in content pages)
5. `package.json` -- scripts and dependencies

Then check `git status` and `git log --oneline -10` for recent activity.

Summarize in a few lines:
- Branch and working tree status
- Any uncommitted or staged changes
- Recent changes (from git log)
- Confirm ready to work
