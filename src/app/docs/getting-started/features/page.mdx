import ExternalLink from '@/components/ExternalLink'
import PageInformationalData from '@/components/PageInformationalData'
import FeaturesGrid from './FeaturesGrid'
import { changelogUrl } from '@/shared/urls'
export const metadata = {
  title: 'Features',
  description: 'Key Koi Features',
}

# Product Features

<PageInformationalData lastModified={lastModified} />

---
<Info>
   Be sure to take a look at our <ExternalLink url={`${changelogUrl}`} linkText="Changelog" /> for recent additions, fixes, and updates to features.
</Info>

<FeaturesGrid />

