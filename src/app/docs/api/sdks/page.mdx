import { Libraries } from '@/components/Libraries'
import PageInformationalData from '@/components/PageInformationalData'
import { baseUrl } from '@/shared/urls'
export const metadata = {
  title: 'Koi SDKs',
  description:
    'Koi SDKs to more deeply integrate Koi into your workflows.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

# Koi SDKs

<PageInformationalData lastModified={lastModified} />

Official SDKs for Koi are <span className="italic text-orange-500">coming soon</span>.

In the meantime, you can use <a href={`${baseUrl}`} target="_blank">Koi</a> to begin modeling instantly. {{ className: 'lead' }}

<Libraries />
